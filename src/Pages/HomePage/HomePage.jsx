import { Helmet } from "react-helmet-async";

const HomePage = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro Boss || Home</title>
            </Helmet>
            Home Page
        </div>
    );
};

export default HomePage;